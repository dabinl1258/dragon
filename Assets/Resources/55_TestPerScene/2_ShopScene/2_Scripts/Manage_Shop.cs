using UnityEngine;
using System.Collections;

public class Manage_Shop : MonoBehaviour {
    int CurrentGold=1000;
    public UILabel label;
	// Use this for initialization
	void Start () {

    }
	
	// Update is called once per frame
	void Update () {
        //gameObject.GetComponent<UILabel>().text = CurrentGold.ToString() + "";
    }
}
